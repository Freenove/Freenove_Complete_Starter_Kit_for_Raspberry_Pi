using Microsoft.Extensions.Hosting; using Microsoft.Extensions.Logging;
var b=Host.CreateApplicationBuilder(args); b.Logging.AddConsole(); b.Services.AddHostedService<11_Thermistor_ThermometerService>(); using var h=b.Build(); await h.RunAsync();
public sealed class 11_Thermistor_ThermometerService(ILogger<11_Thermistor_ThermometerService> log):BackgroundService{ protected override async Task ExecuteAsync(CancellationToken ct){ while(!ct.IsCancellationRequested) await Task.Delay(1000,ct); } }